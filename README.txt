README

To run the code in the terminal and enter in initial values manually use: ant run

To run the code in the terminal using an input file with the initial conditions{
ant compile 
java -classpath jarfilename.jar "mapfilename.dat" "initialconditionfile.txt"
