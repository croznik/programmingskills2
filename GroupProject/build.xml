<project name="Programmingskills2" default="compile" basedir=".">
    <description>
        build file for groupproject
    </description>
  <!-- set global properties for this build -->
 <!-- <property name="src" location="."/>-->
  <property name="build" location="build/classes"/>

  <target name="init" depends="clean">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="src" destdir="${build}"/>
  </target>

  <target name="run" depends="compile">
   <java classname ="groupproject.TestDriver">
    <classpath>
      <pathelement path="${build}"/>
    </classpath>
   </java>
  </target>
 
  <target name="clean"
        description="clean up" >
 
  <!-- Delete the class files from the ${build} directory-->
<!--<delete>
    <fileset dir="${build}" includes="*.class"/>
    <fileset dir="${build}" includes="*~" defaultexcludes="no"/>
  </delete>-->
  <delete dir="build"/>
  </target>
</project>
